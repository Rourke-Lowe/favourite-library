{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "npm install && npm install critters",
          "buildCommand": "npm run build"
        }
      }
    ]
  }